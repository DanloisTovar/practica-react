import '../navbar/navbar.css';
const Footer = ({ informacion }) => {
    return (
        <div className="navbar">
            <center>
                <h1>{informacion}</h1>
            </center>
        </div>
    );
};

export default Footer;
